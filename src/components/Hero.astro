---
import Button from './Button.astro';
---

<div class="hero-grid">
  <div class="hero-copy reveal" style="--delay: 0.05s">
    <h1>Quality strategy for scalable delivery</h1>
    <p class="hero-role">I build quality systems that scale, reduce risk, and keep teams shipping.</p>
    <div class="hero-action-row">
      <div class="hero-actions">
        <Button href="#contact">Start a conversation</Button>
        <Button href="#experience">View experience</Button>
      </div>
      <img class="hero-avatar-inline" src="/logos/avatar.jpeg" alt="Portrait of Anthony Burt" />
    </div>
    <div class="hero-highlights">
      <div>
        <p class="highlight-value">25%</p>
        <p class="highlight-label">Fewer escaped defects through automation and quality gates</p>
      </div>
      <div>
        <p class="highlight-value">50+</p>
        <p class="highlight-label">50+ annual releases for flagship platform; cross-team advisor</p>
      </div>
      <div>
        <p class="highlight-value">8</p>
        <p class="highlight-label">Cross-functional teams aligned on quality standards</p>
      </div>
    </div>
  </div>
  <div class="hero-visual reveal" style="--delay: 0.12s" aria-hidden="true">
    <div class="hero-frame">
      <img class="hero-avatar" src="/logos/avatar.jpeg" alt="" aria-hidden="true" />
    </div>
  </div>
</div>

<style>
  .hero-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 48px;
    align-items: center;
  }

  .hero-copy h1 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: clamp(1.45rem, 2.1vw, 2rem);
    margin: 0 0 8px;
  }

  .hero-copy p {
    margin: 0;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  .hero-role {
    font-size: 1.1rem;
    margin-bottom: 0;
    max-width: 560px;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 18px;
  }

  .hero-action-row {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .hero-avatar-inline {
    display: none;
    width: 120px;
    height: 120px;
    object-fit: contain;
    border-radius: 16px;
    border: 2px solid rgba(244, 245, 247, 0.14);
    background: rgba(15, 11, 12, 0.7);
    box-shadow: 0 10px 18px rgba(0, 0, 0, 0.35);
  }

  .hero-highlights {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 12px;
  }

  .hero-highlights div {
    background: var(--surface);
    padding: 12px 14px;
    border-radius: var(--radius-md);
    border: 1px solid rgba(244, 245, 247, 0.08);
  }

  .highlight-value {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 1.6rem;
    margin: 0 0 6px;
  }

  .highlight-label {
    margin: 0;
    color: var(--muted);
    font-size: 0.95rem;
  }


  .hero-visual {
    display: grid;
    gap: 16px;
    align-content: start;
  }

  .hero-frame {
    background: var(--surface);
    border-radius: var(--radius-lg);
    border: 2px solid rgba(244, 245, 247, 0.14);
    padding: 24px;
    min-height: 320px;
    display: grid;
    gap: 12px;
    box-shadow:
      0 20px 32px rgba(0, 0, 0, 0.4),
      -18px 12px 28px rgba(255, 61, 154, 0.16),
      18px -12px 28px rgba(34, 211, 238, 0.14);
    position: relative;
    overflow: hidden;
  }

  .hero-frame::before {
    content: '';
    position: absolute;
    inset: -20%;
    background-image:
      radial-gradient(circle at 1px 1px, rgba(255, 61, 154, 0.22) 1.5px, transparent 0),
      radial-gradient(circle at 2px 2px, rgba(34, 211, 238, 0.18) 1.2px, transparent 0);
    background-size: 6px 6px;
    opacity: 0.7;
    mix-blend-mode: screen;
    transform: translate(0, 0) rotate(-2deg);
    transition: transform 0.18s ease-out;
    z-index: 0;
  }

  .hero-frame::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(
      circle at 1px 1px,
      rgba(244, 245, 247, 0.12) 1px,
      transparent 0
    );
    background-size: 16px 16px;
    opacity: 0.07;
    pointer-events: none;
    z-index: 1;
  }

  .hero-avatar {
    width: 100%;
    height: 100%;
    object-fit: contain;
    border-radius: 18px;
    display: block;
    position: relative;
    z-index: 2;
  }

  @media (hover: hover) {
    .hero-frame:hover::before {
      transform: translate(6px, -6px) rotate(-2deg);
    }
  }

  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 32px;
    }

    .hero-copy {
      align-items: center;
    }

    .hero-action-row {
      flex-direction: column;
      width: 100%;
    }

    .hero-actions {
      justify-content: center;
      width: 100%;
      order: 2;
    }

    .hero-visual {
      display: none;
    }

    .hero-avatar-inline {
      display: block;
      margin-top: 16px;
      order: 1;
    }
  }
</style>

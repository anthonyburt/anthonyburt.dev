{
  "name": "anthonyburt.dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run lint && npm run format && npm run test && astro build",
    "preview": "astro preview",
    "test": "playwright test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "astro": "astro",
    "ci": "npm run lint && npm run format && npm run test && astro build"
  },
  "dependencies": {
    "astro": "^5.16.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "eslint": "^9.39.2",
    "eslint-plugin-astro": "^1.5.0",
    "prettier": "^3.7.4",
    "prettier-plugin-astro": "^0.14.1"
  }
}
